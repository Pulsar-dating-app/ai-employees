// Twitter/X uses the same card as OpenGraph. Re-export the one generator so
// there is a single image and a single place to change the copy.
export { default, alt, size, contentType } from "./opengraph-image";
